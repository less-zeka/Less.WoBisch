@model SignalRChat.Models.LoginModel

@{
    ViewBag.Title = "Login";
}

<h2>Login</h2>

@using (Html.BeginForm())
{
    @*@Html.HiddenFor(m => Model.Identifier)*@
    @Html.EditorForModel()
    <input type="submit" class="btn" value="Login" />
}